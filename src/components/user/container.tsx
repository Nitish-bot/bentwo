import { Container } from "@/components/ui/container"
import type { ContainerProps } from "@/components/ui/container"

export function UContainer(props: ContainerProps) {
  return <Container {...props} />
}
